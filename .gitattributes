# 기본: 텍스트는 저장소에 LF로 정규화
* text=auto eol=lf

# 셸 스크립트는 반드시 LF. CRLF면 macOS·Linux에서
# "bad interpreter: /usr/bin/env bash^M" 로 실행되지 않는다
*.sh text eol=lf

# 바이너리 — 줄바꿈 변환 금지
*.png binary
*.jpg binary
*.gif binary
*.mp4 binary
*.mp3 binary
